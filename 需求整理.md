# 需求整理：鸣潮·终焉矩阵配队模拟器

> 整理日期：2026-09-06 ｜ 来源：《需求文档.docx》 + 用户补充澄清（三轮）+ 卡片态效果补充（2026-09-06 晚，见 §2.8）+ 素材与角色数据核对（2026-09-10 晚，见 §7、§8）
> 状态：**需求已定稿**（§6 决策全部确认）。**素材已到位（2026-09-10）**——用户已提供 `角色属性对照表.xlsx` 与 `图片素材/` 文件夹；角色基础数据已整理为 `docs/characters.json`。**开工条件已满足**，待用户下发开工指令即可进入正式开发。

## 1. 产品定位

一个以"终焉矩阵"为玩法周期的**组队配置模拟器**：用户从全角色池拖拽角色组成多支 **3 人队伍**，系统依据每个角色的**体力值（=当期可用次数上限）** 约束分配；运营/玩家可按《鸣潮》官方当期矩阵数据，在后台为角色配置"本期强化"。

- 交付形态：**纯前端**，尽量无服务器、浏览器直接运行；将来可租借云服务器/购买域名做静态托管或迁移后端（MySQL 模型仅作参考，见 `docs/db-schema.sql`）。
- 数据持久化：浏览器 localStorage（单机）。
- 界面视觉：**深色为主 + 浅色并存，支持昼夜（明暗）模式切换**（用户确认）。

## 2. 用户端功能需求

### 2.1 拖拽吸附式组队
- 角色卡片可从**角色池**拖到**队伍槽位**，自动吸附到目标位置（参考"从夯到拉"排行榜拖拽交互）。
- 支持**队伍内部拖拽调整顺序**。
- 支持把角色从队伍**拖回角色池**，或点击卡片上的"移除"按钮。
- 拖拽落地规则（拟定）：拖到空槽→入队；拖到已占用槽位（同队）→换序；跨队拖到已占用槽位→提示"槽位已占用，请拖到空位"；拖到角色池空白区→移出队伍。

### 2.2 角色池、队伍区与矩阵总览布局
- **角色池区**：展示全部可用角色，卡片含：头像、名字、属性（图标/配色）、当前体力值、多模态切换按钮、本期强化角标。
- **队伍区**：每支队伍 **3 人**；**可同时显示多支队伍**，一览全局。
  - 队伍支持**自定义命名**；**数量无上限**，可自由新建。
- **全角色矩阵总览（页面内固定区块）**：常驻页面固定区块，同时看到所有角色分配到哪些队伍、哪些未分配；用颜色或标签区分"已入队 / 待分配"。

### 2.3 角色卡片形式（参考：库街区官方角色图鉴）
- 卡片视觉参考 **《鸣潮》官方 WIKI 角色图鉴**（库街区）：`https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1105`。
- 该站为 **JS 动态渲染**，直接抓取仅能取得页面标题、无法解析正文；**素材由用户从官网图鉴自备，打包成素材文件夹后提供**，届时按其内容处理并逐字段复刻（拟含：角色头像、稀有度表现、属性图标、名字等；叠加本项目专属信息：体力值、多模态切换、入队状态、本期强化角标）。
- ✅ **素材已到位（2026-09-10）**，规格详见 **§7**。据实际素材，卡片由以下图层合成（整体基准 151×251，见 §2.8 样例）：
  1. **底 240×320 立绘**（`共鸣者立绘/角色名_立绘.png`），裁切填充卡片上部；
  2. **底部星级光带**（`边框素材/五星.png` 或 `四星.png`，按角色星级取用）——**注意它不是外框，是底部光带**，见 §7.3；
  3. **右上角属性徽记** = `属性图标/属性名.png`（90×90）+ 前端 CSS 圆形底框（圆形底框美术未提供，需前端绘制）；
  4. **底部黑色名牌条 + 白色角色名**（素材未含，需前端以 CSS/文本层绘制）；
  5. 叠加本项目专属信息：体力值、多模态切换按钮、入队状态、本期强化角标。

### 2.4 角色属性体系（已确认）
- 《鸣潮》中每个角色具有**固有属性（共鸣属性）**，共六类：**热熔、冷凝、气动、导电、衍射、湮灭**。
- 属性应在角色卡/角色池中展示（图标/配色区分），并为角色数据的核心字段。

### 2.5 漂泊者（主角）特殊配队规则（已确认，用户强调与多模态不同）
- 漂泊者**体力值仅为 1 点**；本人可**切换为不同属性**登场，现有 **衍射、湮灭、气动、导电** 属性漂泊者，**随游戏版本更新会新增更多可切换属性**（数据模型须可扩展）。
- ⚠️ **不同于共鸣模态切换**：模态切换不改变"以谁上场"的占用语义。
  - 真实模态数据以 `角色属性对照表.xlsx` 的**「共鸣模态」列**为准（2026-09-10 用户补表）：**洛瑟菈 霜渐/声骸、爱弥斯 震谐/聚爆、达尼娅 聚爆/集谐、琳奈 震谐/集谐**（共 4 位角色、5 种模态：霜渐/声骸/震谐/聚爆/集谐）。
  - ⚠️ 本节早先写的"如 爱弥斯-震谐/集谐"只是**举例、并非真实数据**，已按表格更正。
- 规则：使用漂泊者配队时，**必须指定以哪种属性登场（配队时就选好属性）**；整个矩阵方案中漂泊者**合计只能登场体力值（1）次**，不能同时以两种属性出现在不同队伍。
- **呈现方式（用户拍板）**：角色池**按属性拆成多张卡**（漂泊者·衍射、漂泊者·湮灭、漂泊者·气动、漂泊者·导电…），各自可选；**所有属性形态共享同一体力池**（合计仅可登场体力值=1 次）。版本新增属性 → 池中对应新增一张卡。
- 版本迭代：新增可切换属性只是"漂泊者可选卡变多"，不改变共享体力上限语义。

### 2.6 体力约束与提醒
- 每个角色有使用次数上限（= 体力值），**入队一次消耗 1 点**（漂泊者同理，全属性形态合计仅 1 次）。
- 体力耗尽后：角色卡片**变灰、不可拖拽**。
- 若尝试将体力为 0 的角色拖入队伍，系统给出**提示**。

### 2.7 导出 / 分享配队方案
- 需要**导出/分享配队方案**，实现方式**复制文本和链接均可**（含漂泊者所选属性信息）。

### 2.8 卡片态效果：未选中态 / 选中态（2026-09-06 晚补充 · 用户提供效果图）
- **用户提供的效果图**：`未选中效果.png`、`选中效果.png`，样例角色为**漂泊者-男-导电**。两图卡内内容完全一致（立绘、右上属性徽记、底部黑色名牌条白字"漂泊者-男-导电"），**差异仅在描边/发光/整体尺寸**。
- **未选中态**：卡片仅深色底 + 卡片自身美术（含底部金色光带，属原图内容）；**无金色描边、无外发光**，保持基准尺寸。
- **选中态**：
  1. **整卡一圈香槟金细描边**（1–2px），包裹整张卡片外框——**含底部黑色名牌条**，四角同色；
  2. 描边**外扩一层柔和金色辉光**（外发光）；
  3. **整体等比放大**约 6%（效果图 151×251 → 160×260，即约 +10px 宽高）。
- **触发条件（用户拍板）**：**拖拽悬浮时高亮（drag-over）**——拖拽中的卡片悬停到目标卡片上时，目标卡片呈现选中态；离开即恢复未选中态。
  - 同时覆盖**指针悬停（hover）**：鼠标悬停在同一张卡上时呈现同一套选中态视觉（与 drag-over 共用样式，避免两套观感）。
- **生效范围（用户拍板）**：**角色池的卡片**（队伍槽位卡片、全角色矩阵总览卡片本期不做此态）。
- **实现方式（用户拍板）**：**CSS 实现，不需要额外的选中态素材图**。
  - 选中态 = `transform: scale(1.06)`（放大）+ 金色描边（`border`/`outline` 或 `box-shadow` 0 0 0 1–2px 金色）+ 外发光（`box-shadow` 金色柔光）。
  - 因此**素材文件夹只需为每个角色提供一张基础卡图**，无需"选中版"；任意角色通用同一套态样式。
- **状态性质**：hover / drag-over 均为**瞬态**，移开即恢复，**不写入 localStorage、不影响配队数据**；与体力耗尽变灰、本期强化角标等状态可叠加（高亮描边叠在卡片最外层）。

## 3. 期次与本期强化（进阶 · 已澄清）

### 3.1 期次概念
- 数据带"期"维度；每期按《鸣潮》官方**新一期矩阵增强数据**，由**人工在后台手动配置**当期角色强化。
- **不是固定/自动轮换，不做官方数据抓取**（用户原话）。

### 3.2 强化效果建模（用户设计修正）
- 角色强化可能涉及**具体技能变化**，**不能单纯用数值增减**表示。
- 强化效果**直接以字符串输出/存储**，自由文本展示。
- 仅**特殊情况如"体力值+1"额外写 if/else** 做真实体力加成；其余只做文本展示（用户原话：*"不能单纯使用数值增减，可直接改为字符串输出；特殊情况如体力值+1可以额外写一个if else"*）。
- 数据结构示意：`{ text: "效果描述", staminaPlus: 0|1|2… }`，`staminaPlus>0` 走真实体力加成分支。

### 3.3 界面表现
- 强化效果在角色头像/卡片显示为**角标或图标**（"+1" "↑"），一目了然。

### 3.4 体力重置（已确认）
- **切期自动重置全部角色体力**；另保留后台"一键重置当期体力"兜底。

## 4. 后台管理需求（进阶）
- **新增角色**：角色名、属性（六选一）、上传头像、默认体力值、多模态选项。
- **本期强化配置**：为角色添加"本期强化"（字符串描述；可选"体力+X"特殊加成）。
- **一键重置当期体力**。
- **入口口令：暂不需要**（本地工具不设）。

## 5. 数据模型约定

> 完整 MySQL 参考模型见 `docs/db-schema.sql`（**仅作未来云服务器/真后端迁移参考，当前不落地**）。

- 核心表：`characters`、`character_modes`（多模态）、`periods`（期次）、`period_enhancements`（期次强化）。
- `characters` 字段原则（更新）：
  - **核心字段 = 组队 + 体力 + 属性**。属性六选一（热熔/冷凝/气动/导电/衍射/湮灭），`element` 已从"预留"转为核心。
  - **仍预留**：稀有度（rarity）、武器类型（weapon_type）。
- 体力语义：`default_stamina` 为基础属性；**当期剩余/已用次数属"期状态"**，切期重置，不入 `characters`。
- 强化效果字段：`effect_text`（字符串主输出）+ `stamina_plus`（特殊分支）。
- **漂泊者**：体力恒 1；**按属性拆多卡**（漂泊者·衍射 等），各属性形态**共享同一体力池**；随版本新增属性卡。切后端落地时共享体力池需体现"同一角色不同属性卡合计占用"约束。
- 分享链接：编码当前期角色状态与队伍配置（含漂泊者属性形态）为可恢复 URL/文本。

## 6. 需求决策汇总

| # | 问题/事项 | 决策 |
|---|---|---|
| 1 | 切期是否自动重置全角色体力 | **是**，自动重置；另留手动一键重置 |
| 2 | 导出/分享配队方案 | **需要**，复制文本与链接均可 |
| 3 | 队伍命名 / 数量上限 | **需要命名**；**数量无上限** |
| 4 | UI 风格 | **深色为主、白色并存 → 昼夜（明暗）模式切换** |
| 5 | 后台管理口令 | **暂不需要** |
| 6 | 全角色矩阵总览呈现 | ~~**页面内固定区块**~~ → **已取消**（2026-09-10 用户要求「去除底下的全角色矩阵预览」，其「点击自动入队」能力并入右侧角色池：点击卡片即自动放入第一支有空位的队伍） |
| 7 | 角色属性体系 | 角色固有属性六选一：热熔/冷凝/气动/导电/衍射/湮灭，卡片展示，为核心字段 |
| 8 | 漂泊者特殊规则 | 体力仅 1；可切换属性（衍射/湮灭/气动/导电…随版本新增）；与模态切换不同，全矩阵合计仅可登场 1 次 |
| 9 | 漂泊者呈现方式 | **角色池按属性拆多张卡、共享同一体力池**（合计仅登场体力值次）；新版本属性 → 新增卡 |
| 10 | 角色卡片/素材 | 参考库街区官方角色图鉴；**素材由用户自备打包成素材文件夹后提供**，收到后再开发 |
| 11 | 卡片"未选中/选中"两态触发 | **拖拽悬浮（drag-over）时高亮**；移开恢复；同时覆盖指针 hover，共用同一套样式 |
| 12 | 两态生效范围 | **角色池的卡片**（队伍槽位、矩阵总览卡片本期不做） |
| 13 | 两态实现方式 | **CSS 实现**（放大 ~6% + 金色描边 + 金色外发光），**无需选中态素材图**，素材仅需一张基础卡图 |
| 14 | 素材文件夹是否满足开发 | **已满足**（2026-09-10）：60 立绘 + 6 属性图标 + 2 星级光带 + 2 样例图，全透明 PNG，规格见 §7 |
| 15 | 「边框素材」的正确用法 | **不是外框**，是**卡片底部星级光带**（顶部 75% 透明）：五星=金色+5星、四星=紫色+4星，贴卡片底部 |
| 16 | 漂泊者立绘区分 | 四形态**共用同一张立绘**（用户确认）；属性区分靠属性徽记与卡名，不靠立绘 |
| 17 | 角色数据 | 以 **`docs/characters.json`** 为准：**60 角色**（热熔11/导电11/气动11/冷凝9/衍射9/湮灭9），体力=2 者为**白芷/穗穗/卜灵/维里奈/守岸人/莫宁**；表格 4 处已修正，见 §8.3 |

> 需求无未决项。当前处于**等待用户提供官方图鉴素材文件夹**阶段；素材到位后即开始正式开发。

---

## 7. 素材文件夹规格（2026-09-10 核对 · 用户已提供）

> 素材根目录：**`图片素材/`**，共 **70 个文件**（60 张立绘 + 6 张属性图标 + 2 张星级光带 + 2 张样例图）。
> **全部为带透明通道的 PNG**（`Format32bppArgb`），可直接叠加合成，无需抠图。

### 7.1 共鸣者立绘 —— `图片素材/共鸣者立绘/`（60 张）
- **统一尺寸 240×320**（竖向 3:4），文件名 = `角色名_立绘.png`（与角色数据表姓名一一对应）。2026-09-10 用户要求统一加 `_立绘` 后缀，原 `角色名.png` 全部更名。
- 内容为**角色立绘原画**，不含名牌条、不含文字、不含星级光带（这些需前端合成/绘制）。
- ⚠️ **漂泊者四形态（导电/气动/湮灭/衍射）为同一张图**（SHA256 完全相同）。用户确认：*只有这一张通用立绘，四个属性共用它*；配队时以所选属性决定入队语义，立绘本身不区分。

### 7.2 属性图标 —— `图片素材/属性图标/`（6 张）
- **统一尺寸 90×90**，文件名 = `属性名.png`：`热熔 / 冷凝 / 气动 / 导电 / 衍射 / 湮灭`（与六属性枚举完全一致）。
- 图形为居中元素徽记，四周留透明边距（约上 25%、下 75% 为空白），**实际绘制区约在中上部**。
- 立绘右上角的属性徽记以本图标 + 前端圆形底框合成（**样例图上的圆形底框美术未单独提供**，需前端用 CSS 画）。

### 7.3 星级光带（名为"边框素材"，实为卡片底部光带）—— `图片素材/边框素材/`（2 张）
> ⚠️ **重要澄清：这两张不是卡片外框，不要当外框用。**
- 尺寸 **360×360**，但经逐行 alpha 扫描确认：**顶部约 75% 区域完全透明**，只有**底部约 25% 是不透明的波浪纹 + 星数**。
- 用法：**贴在卡片底部**作为稀有度表现 + 底部光效（与样例图卡片下部的金色光带吻合）。
- 两张的差异 = **稀有度配色与星数**：
  - `五星.png`：**金色**波浪光带 + **5 颗星**；
  - `四星.png`：**紫色**波浪光带 + **4 颗星**。
- 现有素材仅四星/五星两档，与数据表星级分布（4★/5★）吻合；将来若出现其他档位需新增。

### 7.4 样例图 —— `图片素材/样例/`（2 张）
- `未选中效果.png` **151×251**、`选中效果.png` **160×260**，即 §2.8 的两态效果图，作为卡片最终视觉基准。
- 样例卡结构：上部为立绘（含底部金色光带）+ 右上属性徽记，下部为**黑色名牌条 + 白色角色名**。
- ⚠️ **名牌条与角色名文字不在素材内**，需前端用 CSS/文本层绘制。

## 8. 角色数据核对（2026-09-10）

> 数据源：用户提供的 `角色属性对照表.xlsx`（单表，4 列：角色名 / 星级 / 属性 / 体力值，共 61 行数据行）。
> 整理产物：**`docs/characters.json`** —— 已清洗、校验、可直接作为前端角色池初始化数据。

### 8.1 最终名单规模
- **角色总数 60**（表格 61 行，剔除 1 行重复后）。
- **属性分布**：热熔 11、导电 11、气动 11、冷凝 9、衍射 9、湮灭 9。
- **星级分布**：5★ 48、4★ 12（素材仅这两档，吻合）。
- 60 个角色**全部有对应立绘**，无缺失、无多余。

### 8.2 体力值规则（修正后）
- 默认体力值 = **1**；以下 **6 位为 2**（奶妈/辅助）：
  **白芷、穗穗、卜灵、维里奈、守岸人、莫宁**。
- 漂泊者全部属性形态合计仅 **1** 次（见 §2.5）。

### 8.3 表格修正记录（均已确认）
| # | 类型 | 内容 |
|---|---|---|
| 1 | 重复行 | 表格中「**漂泊者·气动**」出现 **2 行**（第 33、39 行）。用户确认*就是多写了一份，直接删掉* → 气动角色由 12 项修正为 **11** 项，总数 61 → **60**。 |
| 2 | 数值修正 | 「**卜灵**」（4★ 导电）体力值由表格的 1 修正为 **2**（用户口头补充）。 |
| 3 | 错别字 | 表格写「**达妮娅**」，立绘文件名是「**达尼娅**」，两者不一致。统一采用「**达尼娅**」（以文件名为准）。 |
| 4 | 数值修正 | 「**莫宁**」（5★ 热熔）体力值由 1 修正为 **2**（用户 2026-09-10 指出：「莫宁应默认有两点体力」）。体力=2 者由 5 位增至 6 位。 |

### 8.4 数据模型口径
- 本数据为**基础数据**，**不含**当期剩余/已用次数——后者属「期状态」，切期重置，不入角色基础表（与 §5 约定一致）。
- 漂泊者四张卡以 `costGroup: "rover"` 标记**共用体力池**，实现"合计仅可登场 1 次"的约束。
- 多模态选项（如 爱弥斯-震谐/集谐）为**独立机制**，不改变"以谁上场"的占用语义，不体现在本表。
